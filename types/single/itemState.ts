export interface EquipmentState {
  name: string;
  description: string;
  statDescription: string;
  imageString: string;
}

export interface ConsumableState {
  name: string;
  description: string;
  statDescription: string;
  imageString: string;
}
